<h2 class="title-lg">Категории</h2>
<div>
  <% @categories.each do |category| %>
    <div>
      <%= render category %>
    </div>
  <% end %>
  <hr>
  <%= link_to 'Добавить категортю', new_category_path %>
</div>
